package com.vdn.lampbearer.attributes.occupation;

import com.vdn.lampbearer.attributes.Attribute;

/**
 * An entity which has this attribute owns the Tile it's on. Nothing/nobody else can move to it.
 */
public interface BlockOccupier extends Attribute {
}
